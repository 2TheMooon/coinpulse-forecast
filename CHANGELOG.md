# CoinPulse Forecast — Changelog

A running log of changes, especially the automated daily calibration runs that
tune and improve the forecasting model.

## Unreleased

### Added
- Monte-Carlo forecasting engine (GBM + Student-t fat tails), probability cone,
  scenario targets, touch probabilities, analytic cross-check.
- Walk-forward **calibration backtest** (PIT coverage of the cone).
- Triple-source data layer: CryptoCompare → Binance → deterministic synthetic.
- Interactive fan chart with sample paths, date axis, hover readout.
- Persistence, coin filter, per-coin live/demo dots, 3-min auto-refresh.
- Node-requireable engine + headless backtest harness (`backtest/run-backtest.js`).

<!-- Daily calibration runs append below this line -->

- 2026-08-14 AI daily: **the tournament's "Shipped engine" row was never scoring the shipped engine — it hard-coded `driftDamp: 0.55`.** One-line fix in `backtest/run-backtest.js` (`calibrateEntry`, baseline branch): dropped the `driftDamp: 0.55` override so `backtestCalibration` falls back to `MODEL.driftDamp`, the knob the auto-tuner actually moves. `forecast.js` is untouched apart from the tuner's own `driftDamp: 0.3 → 0.25` line; `MODEL` is (volPremium 0.85, driftDamp 0.25, tDof 5, longHorizonBoost 0.30). Evidence this is a stale literal and not a deliberate reference point: `git log -S` dates the line to the fork's **first** commit `5610489` (2026-06-06), and `forecast.js` at that same commit `e446a75` carried `driftDamp: **0.4**` — so 0.55 never matched the shipped value, not even on day one, while the tuner has since walked the real knob 0.4 → 0.55 → 0.4 → 0.3 → 0.25. The rest of that options object (`volScale: 1`, `fatTails: true`) just spells out defaults — and `fatTails` is not even read by `backtestCalibration` — which is the signature of a frozen defaults-copy, not a chosen baseline; nothing in the report labels the row as anything but "Shipped engine (forecast.js)". Consequence, and why this outranked every model lever today: the auto-tuner grades `forecast.js` at `MODEL.driftDamp` (`auto-tune.js:135`, `backtestCalibration(candles, {horizon: h})`, same HORIZONS `[7,14,30]`, same `HISTORY_LIMIT` 200, same 14 coins, same window-weighting), so **the two halves of the daily loop have been grading two different configurations of the same engine for 70 days** — and the loop's own numbers say so: today the tuner reports cov80 **80.8** / meanPit **0.515** while the tournament's window-weighted baseline reports cov80 **78.47** / meanPit **0.5586** (weights 1036/994/882). The 08-10 run attributed that gap entirely to the coinbase history-depth bug; `capHistory` fixed that (today's report is `binance ×14` at exactly 200 candles) and a 2.3-point / 0.044-PIT gap survived, because there was a **second, independent** cause. This also settles today's most interesting negative result: the tuner moved damp 0.4 → 0.3 on 08-13 specifically to pull PIT down, and today's report moved the wrong way (7d/14d/30d spread over `ewma_t_zero` 0.046/0.079/0.107 → 0.056/0.083/0.113) — not a failed hypothesis, but an **unmeasurable** one, since the row was pinned at 0.55 throughout. It follows that the 08-12 entry's linear interpolation ("damp 0.4 → PIT 0.573, damp 0 → 0.475, so PIT-neutral damp ≈ 0.10") was computed on a row that actually used 0.55, and yesterday's plan to "measure damp 0.3 tomorrow" was unmeasurable by construction; both are retracted. Verified the override was binding and that removing it moves the number the right way — on a 24-series synthetic panel (vol 0.03–0.075 × log-drift −0.004…+0.005, 200 candles) `driftDamp: 0.55` vs the shipped 0.25 reports meanPit 0.548/0.568/0.612 vs 0.540/0.557/0.595 and cov80 72.18/75.47/78.57 vs 72.41/76.76/79.83 at 7/14/30d, i.e. correcting it lowers reported PIT and raises reported cov80, the same direction as the observed tuner-vs-tournament gap (smaller in size only because the synthetic drifts are milder than real crypto's). **Expect tomorrow's numbers to step**: the baseline row will move toward the tuner's — lower meanPIT, higher cov80 — and that is the honest value, not an improvement. Shipped-engine calibration as reported today (still under the stale 0.55): cov80 **79.3 / 79.0 / 76.9**, meanPIT 0.545 / 0.552 / 0.582; overlap-corrected `nEff` = 148 / 71 / 29.4 gives se80 = 3.3 / 4.8 / 7.4, so the deviations (−0.7 / −1.0 / −3.1) are 0.21 / 0.21 / 0.42 SE — coverage is within tolerance at every horizon and no model change is indicated on cov80 grounds. Declined `switch → momentum` for the **eighth** time (today is a `keep`; momentum leads by 1.5 points, inside the 3-pt margin, and still collapses at 30d — cov80 65 vs the live row's 76.9 — where routing already sends traffic to `block_boot`). Deliberately NOT done, but **quantified offline today and pre-registered as the top candidate for the next run**: `forecast.js` applies the Itô/convexity correction to a drift that is *already* in log space. `quickMuSigma`/`computeStats` set `muDaily = mean(logReturns)`, i.e. the log drift, and then `simulate` uses `drift = mu − kappa` and `analyticTerminal`/`backtestCalibration` use `drift = (mu − 0.5σ²)·H` — so the cone's median is pushed down by ≈`0.5σ²H` (**−3.7% at 30d** on typical crypto vol) for no modelled reason, since κ converts an *arithmetic* drift to a log drift and this drift never was arithmetic. Measured, not argued: re-running `backtestCalibration`'s own loop (reimplementation byte-verified against the shipped function, PIT 0.661484 / cov80 83.871 identical) over 40 seeded **perfectly driftless** log-random-walks — ground truth meanPIT = 0.500 exactly — the shipped form reports **0.518 / 0.526 / 0.545** at 7/14/30d while dropping the `0.5σ²` term reports **0.504 / 0.503 / 0.504**. The artifact scales with σ² as the mechanism demands (0.010/0.015/0.027 at σ=0.03; 0.023/0.036/0.063 at σ=0.07) and is horizon-growing exactly like the +0.056/+0.083/+0.113 bias seen in all 68 reports. Critically for safety, cov50/cov80/cov90 move by ≤1pp in the same test, so the fix is PIT-only and does not disturb the widths the tuner has spent months setting; on real numbers it would take PIT to ≈0.527/0.526/0.537 — toward 0.50 at every horizon with **no overshoot below it**. It is also ~4× the entire `driftDamp` lever, which at realistic |μ_log| ≈ 0.001 can only move 30d PIT by ~0.004 per 0.1 of damp — which is why the tuner has been unable to fix this and has been walking damp downward against the wrong target. Not done today for one reason: it is a model change, and until this run the metric that would judge it was mis-specified. Measurement first, then the model. (Sequencing note for the next run: fix it in all three sites at once — as the 07-24 term-structure fix did — and preserve `simulate`'s kappa so selftest #2 keeps genuinely testing it, i.e. take `muLog` as the damped log drift, set `mu = muLog + kappa` and `drift = muLog`; `app.js:831` already prefers `sim.drift` so no UI change is needed. Note `dirAccuracy` will change meaning: today `drift` is almost always negative, so `predUp = drift >= 0` has been reporting "how often price fell", not directional skill. The same double-correction sits in `strategies.js` (`gbmNormal`/`gbmT`/`momentum`/`garchLite`) — deliberately left alone, since that is the challenger lab and one change per run.) Verified offline: `node backtest/selftest.js` 3/3 (martingale 0.976) exit 0, `node -e "require('./forecast.js')"` exit 0, `node --check backtest/run-backtest.js` exit 0; both throwaway probe scripts deleted, so the committed diff is 8 added / 1 removed lines in one harness file. Public API, `MODEL`, candle shape and `strategies.js` exports all intact.

- 2026-08-14 auto-tune: volPremium 0.85→0.85, driftDamp 0.3→0.25 (calibration error 24.24→23.33, cov80→80.8%)

- 2026-08-13 AI daily: **no change — yesterday's fix fired, and the honest move is to measure it before touching anything else.** The widened `DAMP_GRID` landed its first decision on its first run: the auto-tuner moved `driftDamp` **0.4 → 0.3** (aggregate calibration error 25.21 → 24.62, improvement 0.59 clearing the 0.5 margin, best cov80 81.1%, meanPit 0.518 vs 0.532 on 08-12) — the first non-zero tuner improvement since **2026-07-06**, ending a 38-day / 18-run streak of `improvement 0`. Two details confirm the diagnosis rather than merely the outcome: the tuner selected **0.3, not the new floor 0.2**, so the damp axis is now searching an *interior* optimum instead of pinning at a corner — the grid, not the model, had been the binding constraint, exactly as pre-registered; and it moved `volPremium` **not at all**, so the improvement is attributable to the drift term alone. Shipped-engine calibration today (3rd clean 200-day/binance day, and today's report still reflects `damp 0.4` — `daily.js` runs the tournament *before* the tuner): cov80 **80.9 / 80.6 / 78.0** (7d/14d/30d), meanPIT 0.530 / 0.553 / 0.580, zero-drift control 0.485 / 0.473 / 0.472, drift spread **+0.045 / +0.080 / +0.108** — the same one-sided spread now seen in 67/67 reports at 30d. Coverage is within tolerance at **every** horizon and by a wide margin: overlap-corrected `nEff` = 148 / 71 / 29.4 gives se80 = 3.3 / 4.8 / 7.4 points, so the deviations (+0.9 / +0.6 / −2.0) are **0.27 / 0.13 / 0.27 SE** — the 30d shortfall that looks like a defect in the table is a quarter of one standard error and is not evidence of anything. Why no second change today: the pre-registered next lever is `VOL_GRID = gridRange(0.85, 1.45)` against `VOL_BOUNDS = [0.7, 1.8]` — the same cover-your-own-bounds bug on the vol axis — and the 08-12 entry gated it on *two* conditions, **both of which still fail**. (1) "No measured cov80 miscalibration to justify it": still true, and now quantified above at ≤0.3 SE everywhere. (2) "Revisit once the damp axis settles": it moved for the *first time* today and has **zero** tournament observations; tomorrow's report is the first measurement of `damp 0.3`. The interaction is not hypothetical — the tuner's objective weights PIT 200×, `volPremium` enters PIT only through the −0.5σ²H median term, and `driftDamp` has just shifted the PIT level that the vol axis would be optimising against, so searching the vol axis *now* would be searching against a stale target. The evidence is also far weaker than the damp case ever was: damp pinned at its floor 37/37 runs with a 66-run mechanistic control measurement behind it, whereas `volPremium` has chosen 0.85 in 19 of 20 runs but broke *upward* to 0.9 on 07-15 — a floor that is escapable upward is evidence the optimum sits near 0.85, not far below it. And the risk is asymmetric: lowering `volPremium` narrows the cone, and "too narrow (overconfident)" is the user-harmful failure direction, so opening a downward search with zero measured over-coverage would be speculative. Declined `switch → momentum` for the **seventh** time; today is a `keep` (momentum leads by 2.1 points, inside the 3-pt margin). Noted honestly against my own position: momentum has now edged the live engine four clean-regime days running (−0.6, −3.5, −0.8, −2.1) after averaging **+4.27 points worse** across all 66 reports (sd 5.03), so this is a real short streak — but its entire edge is 7d/14d PIT, precisely the bias `damp 0.3` attacks, and it still collapses at 30d (cov80 **67.2** and cov90 78.0 vs live's 78.0/86.7), while all five prior `switch` flags (06-16, 06-18, 07-06, 07-18, 08-11) mean-reverted within a day. If the gap persists *after* `damp 0.3` is measured, that is a genuine finding; adopting it today would be adopting noise. No source file touched: `forecast.js` is byte-identical to `HEAD` apart from the tuner's own `driftDamp: 0.4 → 0.3` line, and `app.js`, `data.js`, `index.html`, `styles.css`, `strategies.js`, `run-backtest.js`, `auto-tune.js` and `selftest.js` are unmodified; `MODEL` is now (volPremium 0.85, driftDamp 0.3, tDof 5, longHorizonBoost 0.30) with `longHorizonBoost` correctly preserved by `rewriteModelBlock`; public API and candle shape intact. Verified offline: `node backtest/selftest.js` 3/3 (martingale 0.976) exit 0, `node -e "require('./forecast.js')"` exit 0. Pre-registered for the next run, in order: (1) after tomorrow's `damp 0.3` measurement lands, re-check whether the residual PIT spread wants damp 0.25/0.2 — let the tuner decide, no code needed; (2) only once PIT is inside ±0.03 of 0.50, extend `VOL_GRID` to cover `VOL_BOUNDS` at *both* ends (0.70…1.80), since the upper edge 1.45 is short of 1.80 too; (3) `backtestCalibration` scores a lognormal PIT while `simulate()` draws capped Student-t shocks — defensible at 30d by CLT, weakest at 7d, and worth quantifying before it is worth changing.

- 2026-08-13 auto-tune: volPremium 0.85→0.85, driftDamp 0.4→0.3 (calibration error 25.21→24.62, cov80→81.1%)

- 2026-08-12 AI daily: **un-jammed the auto-tuner — its `driftDamp` search grid did not cover its own declared bounds, so the tuner has been a structural no-op for 37 days.** One-line change in `backtest/auto-tune.js`: `DAMP_GRID [0.4, 0.55, 0.7]` → `[0.2, 0.25, 0.3, 0.4, 0.55, 0.7]` (plus a 7-line comment recording why). `DAMP_BOUNDS` already permitted `[0.2, 0.95]` and is **unchanged**; `MIN_IMPROVE` (0.5), `VOL_BOUNDS` and `forecast.js` are untouched, so this hands a decision to the deterministic tuner rather than making one. Evidence that the grid, not the model, was the binding constraint: every `tuning-*.json` since **2026-07-06** — 17 recorded runs spanning 37 days — reports `improvement 0` with `best == current == (volPremium 0.85, driftDamp 0.4)`, i.e. the search has terminated at the exact lower-left **corner** of its own grid every single time, while over that same stretch `meanPit` wandered 0.523 → 0.569 → 0.532 and aggregate error 22.9 → 29.4 → 24.9 with the tuner unable to respond. Evidence that the drift term is what biases PIT, and that it is regime-independent: measuring the shipped engine's meanPIT *relative to* the `ewma_t_zero` control isolates drift from the window set, and across **all 66 tournament reports (06-06 → today)** that spread is positive in 66/66 runs at 30d (0.053–0.122, mean ≈ 0.090), 66/66 at 14d (0.020–0.098) and 65/66 at 7d — it survives the coinbase→binance feed change, the 07-24 term-structure fix and the 08-10 `capHistory` fix, so it is not the artifact the 08-09/08-10/08-11 runs were right to rule out elsewhere. Today: live 30d PIT 0.573 against a control at 0.475, spread +0.098; linear interpolation between `damp 0.4` (0.573) and `damp 0` (≈0.475) puts the PIT-neutral damp at ≈**0.10**, which is *below* `DAMP_BOUNDS[0]`, so the new grid floor of 0.2 **cannot overshoot** — the worst case is landing 30d PIT at ≈0.524 instead of 0.573, an improvement at every horizon, and 20% of the drift is still carried (the engine does not collapse to the zero-drift strategy). Verified the widened grid is not a one-way ratchet: re-ran the tuner's own `aggError` loop offline over all six damp values on 14 synthetic GBM series (200 candles, deterministic seeds) where the fitted drift is genuinely present — `damp 0.4` still wins (err 39.00) and `0.2` is *worse* (40.17), so the tuner descends only when real data supports it. Cost is negligible: the grid goes 13×3 → 13×6, measured at 18 ms per grid point (~1.4 s total, up from ~0.7 s). This is the lever the 08-11 run pre-registered as its top candidate; it gated on "≥3 independent clean-regime days", which today (day 2, and windows overlap ~199/200) does not satisfy — but that gate was for judging the *live PIT level*, whereas the drift *spread* and the corner-pinning are both 66-run, two-regime facts that do not depend on the clean-window count, and the change is to the search grid, not the model. Declined `switch → momentum` for the sixth time on the standing rationale (today it leads by only 0.2 points, inside the 3-pt margin; it collapses at 30d, cov80 66.3 vs live's 77.4, and 30d already routes to `block_boot`). Shipped-engine calibration today: cov80 **80.3 / 79.7 / 77.4** (7d/14d/30d), meanPIT 0.539 / 0.544 / 0.573, tuner improvement 0 (kept 0.85 / 0.4 under the old grid — the new one takes effect tomorrow). Noted, deliberately NOT done: `VOL_GRID` starts at 0.85 while `VOL_BOUNDS` permits 0.7, and `volPremium` is pinned at *that* edge too — the same class of bug on the other axis, but cov80 is currently on target (80.3 aggregate), so there is no measured miscalibration to justify moving two levers in one run; revisit after the damp axis settles. `forecast.js`, `app.js`, `data.js`, `index.html`, `styles.css`, `strategies.js` and `run-backtest.js` untouched; `MODEL` unchanged (volPremium 0.85, driftDamp 0.4, tDof 5, longHorizonBoost 0.3); public API and candle shape intact. Verified offline: `node backtest/selftest.js` 3/3 (martingale 0.976) exit 0, `require('./forecast.js')` exit 0, `node --check` on `auto-tune.js` and `run-backtest.js` exit 0.

- 2026-08-11 AI daily: no new model change — **recovered yesterday's harness fix, which had been silently lost, and broke the rebase doom-loop that lost it.** The working copy arrived mid-interactive-rebase (`.git/rebase-merge`, step 1 of 2) replaying the 08-09 AI commit `aca4c8c` onto `origin/master` @ `3e51d37`; it had halted on add/add conflicts in `backtest/reports/2026-08-09.json`/`.md`, leaving 120 unresolved conflict-marker lines in committed report data (invalid JSON). Critically, the *unapplied* second pick `b3ff1d6` carried yesterday's 13-line `capHistory` fix — so that fix was absent from the working tree, from `HEAD`, and from every report generated since. Root cause of the loop (now its fourth occurrence: 07-31, 08-09, 08-10, today): the cloud workflow and this machine both commit `backtest/reports/<date>.json|.md` for the same date, so the moment one `git push` fails the branches diverge and every subsequent `git pull --rebase` in `ai-daily.cmd` halts on add/add and strands the repo — which fails the next push, and so on. Repair: preserved the stranded work as branch `ai-stranded-2026-08-11`, aborted the rebase, realigned `master` to `origin/master` @ `3e51d37`, restored the harness fix plus 7 local-only `tuning-*.json` records and the 08-09/08-10 changelog entries, and **kept origin's published 08-09/08-10 reports rather than overwriting them** with the local duplicates (the local pair stays reachable on the safety branch). All 103 report JSONs parse; no conflict markers remain anywhere. Independent re-verification of yesterday's diagnosis, straight from the data rather than inferred from PIT: **every committed report 07-23 → 08-10 is `coinbase ×14` carrying 275–350 candles while declaring `config.historyDays: 200`; today's is `binance ×14` carrying exactly 200.** The premise of the fix is therefore confirmed outright. First two clean-regime observations (08-10 stranded, 08-11 today) are stable and mutually consistent: live cov80 78.7/79.4/76.8 then 80.4/80.6/77.9, meanPIT 0.550/0.555/0.585 then 0.537/0.556/0.585 — coverage on target at 7d/14d, −2.1 at 30d. The `ewma_t_zero` control now sits at 30d PIT 0.474/0.473 (near-unbiased) while the live engine's drift adds its usual **+0.111/+0.112** spread — unchanged from the 0.082–0.122 it has always shown. That reframes the 08-09 verdict without contradicting its measurement: the spread was never the problem; under the contaminated ~350-day window the sample's realized uplift pushed the control down to 0.39–0.40 and absorbed the engine's +0.11, landing it near 0.50, whereas the honest 200-day window does not — so the ≈+0.085 30d PIT bias is real and now visible, not a market artifact. Declined `switch → momentum` again (5th time): its 3.3-point mean-score lead is entirely 7d/14d PIT, it collapses at 30d (cov80 68.1 today, 66.4 on 08-10, vs live's 77.9/76.8), 30d is already routed to `block_boot`, and across 24 scored runs live beats momentum by a mean 4.9 points — today's +3.3 is a ~1.9σ excursion the wrong way, and every prior switch flag (06-16, 06-18, 07-06, 07-15, 07-18) mean-reverted within a day. Deliberately NOT done today, in priority order for the next run: (1) `auto-tune.js` has `DAMP_GRID = [0.4, 0.55, 0.7]` while `DAMP_BOUNDS` permits `[0.2, 0.95]`, and `driftDamp` has been pinned at the grid's **lowest** value 0.4 for weeks — the tuner structurally cannot search its way to the smaller drift the 30d PIT now calls for, so extending the grid downward is the obvious next lever, but it needs ≥3 *independent* clean days (08-10 and 08-11 share ~199/200 of their window set, so they are ≈1 observation, not 2); (2) hardening `ai-daily.cmd` against stranding — an untested change to the unattended driver is riskier than the failure it prevents, and today's realignment already restores the push path that breaks the loop. `forecast.js`, `app.js`, `data.js`, `index.html`, `styles.css` and `strategies.js` are byte-identical to `origin/master`; `MODEL` unchanged (volPremium 0.85, driftDamp 0.4, tDof 5, longHorizonBoost 0.3); public API and candle shape intact. Verified offline: `node backtest/selftest.js` 3/3 (martingale 0.976) exit 0, `require('./forecast.js')` exit 0, `node --check` on `run-backtest.js` and `auto-tune.js`, and a `capHistory` unit test extracted from the shipped source (350→200 keeping the most-recent slice, 291→200, 200→200, 150→150, 0→0, null/undefined pass-through).

- 2026-08-10 AI daily: **harness bug fix — the tournament was silently scoring a different history depth depending on which data feed answered.** `run-backtest.js` asks every feed for `HISTORY_LIMIT` (200) days, but Coinbase's `products/…/candles?granularity=86400` endpoint has no limit parameter and returns ~350. CryptoCompare, Binance and CoinGecko all honour the request, so on days the Coinbase fallback answered (the GitHub-runner path, since CC/Binance block those IPs) the tournament scored a ~350-day window set while still recording `config.historyDays: 200`. Fix: added `capHistory()` and applied it to all four `fetchCandles` return sites, so every source is truncated to the last `HISTORY_LIMIT` candles. Evidence this is real and not a market effect — over the last 30 runs the shipped engine's meanPIT separates **perfectly by feed, with non-overlapping ranges**: coinbase days (n=26) 30d PIT 0.445–0.520 (mean 0.483), binance days (n=4) 0.572–0.589 (mean 0.581). The binance days are *interleaved* (07-15, 07-18, 08-09, 08-10), so a time/regime explanation is ruled out — 07-15 and 07-18 sit between coinbase days at 0.45. Corroboration: `auto-tune.js` fetches only CryptoCompare/Binance and therefore always sees exactly 200 days; its meanPit has read 0.54–0.57 **every run for a month** (07-15 0.545 … 08-08 0.544, 08-09 0.540, 08-10 0.541), agreeing with the binance-day tournament and never with the coinbase-day tournament. So the two halves of the pipeline were grading `forecast.js` on different data, and the "PIT excursion" flagged on 08-09 and again today (0.55/0.56/0.59) was never a market artifact or a model regression — it was the tournament falling back to Binance and finally reporting the same number the tuner always had. This also means the daily cov80/meanPIT series was not a time series at all but two interleaved regimes, which is what led the 08-09 run (and, on the same evidence, this one initially) into a phantom diagnosis. **Expect the numbers to step after this fix**: reports will settle on the true 200-day values (meanPIT ≈ 0.55, 30d cov80 ≈ 77) rather than the flattering ~350-day ones; that is the honest baseline, and the now-visible ~+0.05 PIT bias is the auto-tuner's `driftDamp` lever to work, not an AI-owned one. No model change: `forecast.js` untouched, `MODEL` unchanged (volPremium 0.85, driftDamp 0.4, tDof 5, longHorizonBoost 0.3), public API and candle shape intact; the diff is 13 lines in the backtest harness. Tournament today says KEEP → `live_engine` (mean score 29.3; best challenger Momentum 26.6, gap 2.7, inside the 3-pt margin), and momentum was declined again on the standing rationale — it collapses at 30d (cov80 66 vs live's 77). Verified offline: `node backtest/selftest.js` 3/3 (martingale 0.976) exit 0, `require('./forecast.js')` exit 0, `node --check` on both harness files, and a unit test of `capHistory` extracted from the shipped source (350→200, 291→200, 200→200, 150→150 unchanged, null-safe). (Housekeeping: the working copy again arrived mid-rebase — the 08-09 commit `056d118` was stuck replaying onto `origin/master` @ `b28f3b0` with 7 conflicted report files, and the working tree held 4 report files corrupted by unresolved conflict markers, i.e. invalid JSON. Resolved every conflict in favour of origin's committed generator output, completed the rebase, and restored today's fresh artifacts. All 101 report JSONs now parse; no conflict markers anywhere in the repo.)

- 2026-08-09 AI daily: no change — calibration within tolerance; the apparent PIT excursion is a market artifact, not a model regression. Coverage is the healthiest it has been all cycle and confirms the 07-24 term-structure fix held: shipped-engine cov80 79.2 (7d, −0.8) / 80.6 (14d, +0.6) / 77.8 (30d, −2.2), versus ~75.5/77.5/80 before that fix. Tournament says KEEP → `live_engine`. The only red flag was meanPIT jumping to 0.54/0.56/0.59 (from 0.51/0.51/0.52 on 08-06), which spiked the 30d score 23.9→42.3 — but this is **not** attributable to `forecast.js`. Diagnostic: measure the live engine's PIT *relative to* the zero-drift control (`ewma_t_zero`), which isolates the drift term from the window set. That 30d spread is **+0.112 today vs a +0.100 ± 0.011 mean over the last 30 runs** — i.e. the engine's drift contribution is statistically unchanged; what moved was the control itself (0.40 → 0.47/0.48 between 08-06 and 08-08), and every drift-carrying strategy jumped in lockstep by the same ~0.07 (gbm_normal 0.50→0.58, gbm_t 0.50→0.58, garch 0.50→0.58, live 0.52→0.59) while zero-drift/OU did not. That signature is a change in the realized window set (a stretch of recent history reverting upward against a negative fitted drift), not a defect in the shipped cone. It is also weaker evidence than it looks: with a 200-day rolling history and ~45–60 anchors, 08-08 and 08-09 share nearly all of their windows, so two "consecutive" runs are ≈one independent observation. And the drift lever itself is auto-tuner-owned: the tuner ran today, grid-searched `driftDamp`, kept 0.4 (improvement 0), and its own aggregate error has been *falling* for four straight runs (26.80 → 26.41 → 25.77 → 25.19). Also declined the runner-up idea: momentum's 25.7-vs-28.7 lead is exactly the 3.0-point margin (not beyond it) and is entirely PIT-driven — it under-covers badly at 30d (cov80 67 vs live's 78), so adopting it would trade a 2-point-off horizon for a 13-point-off one; the identical flag was audited over 40 days and declined on 07-18. No safe, offline-verifiable model lever remains, so: conservative no-op. selftest 3/3 (martingale 0.976) + require both exit 0. (Housekeeping — real repo repair: the working copy arrived mid-interactive-rebase onto `4bc41bb`, and the stuck commit `f119130` carried **34 unresolved conflict markers** in `backtest/reports/2026-07-31.json`/`.md`, leaving committed report data as invalid JSON. That commit contained only generated reports, all of which `origin/master` already had in clean form through 08-05. Aborted the rebase, realigned `master` to `origin/master` @ `d0dd34b`, preserved the two tuning records that existed only locally (`tuning-2026-07-31`, `tuning-2026-08-03`) and today's fresh artifacts. All 98 report JSONs now parse; no conflict markers remain anywhere in the repo. No code was lost — `master` and `origin/master` were byte-identical outside `backtest/reports/`.)

- 2026-07-24 AI daily: **term-structure fix for persistent short-horizon under-coverage.** Reparameterized the `horizonFactor` (in `simulate`, `analyticTerminal` AND `backtestCalibration`) from `1 + boost·max(0,(h−7)/23)` to `1 + boost·(h/30)`, keeping `longHorizonBoost` at 0.30. The old form applied **zero** width boost at 7d (factor exactly 1.000), so the short end had only a plain sqrt-time normal band — but the calibration scores a normal model, and at short horizons daily fat tails haven't averaged toward normal (CLT), so realized prices spilled outside the 80% band ~24% of the time. Result: the shipped engine's 7d cov80 has sat at ~75.5 (73–78, essentially never reaching 80) for 2+ weeks, with 14d ~77.5 (both light) while boosted 30d is on-target (~80). This is exactly the "cov80 persistently far from 80 beyond what volPremium fixes" case: `volPremium` is global (auto-tuner-owned) and can't lift 7d without over-widening the on-target 30d — the tuner ran today and applied no change (improvement 0, best cov80 78.9), confirming the term structure is the only lever. The new form **pivots on 30d**: factor at 30d is unchanged (1.300, so the on-target horizon is preserved exactly), 7d rises 1.000→1.070 (+7% width) and 14d 1.091→1.140 (+4.5%). By the normal approximation this moves 7d cov80 ≈76→79 and 14d ≈77→79.5 toward target without overshoot. Single-parameter change (no new knob, no API-shape change); self-correcting via the daily loop (tomorrow's network backtest measures it and the tuner re-optimizes `volPremium` around the new term structure). Also updated the mirrored `longHorizonBoost` comment in `auto-tune.js` so the tuner's MODEL-block rewrite stays accurate. Verified offline: `selftest` 3/3 (martingale 0.976) + `require('./forecast.js')` both exit 0; MODEL-block regex still round-trips (lhb 0.30 preserved). (Housekeeping: aborted a leftover interrupted rebase and realigned `master` to `origin/master` @ 8ae64a2 — the stuck commit was a code-free "no change" report recording, superseded by 4 newer calibrations.)

- 2026-07-18 AI daily: no change — declined the tournament's `switch → momentum` recommendation after auditing the full 40-day record; it is a transient-drift artifact, not a durable edge. Today momentum leads by 5.4 score points (22.5 vs live_engine 27.9, outside the 3-pt margin), the largest gap yet, BUT the day-by-day history shows momentum BEATS the shipped engine on only ~5 of 40 days (06-16, 06-18, 07-15, today) and LOSES on the other ~85% — often heavily (gaps −5 to −20). Every momentum win coincides exactly with days the live engine's meanPIT is transiently elevated to 0.52–0.58 (recent upward-drift windows: 07-05 −1.8, 07-09 −2.1, 07-10 −0.1, 07-15 +2.5, today +5.4), because momentum drifts off the recent-14-day mean and only aligns with realized drift in such windows; on the ~0.45–0.48-PIT majority it drift-chases and calibrates worse. Momentum is also chronically overconfident at 30d (cov80 69% today vs live's 81%) since it lacks the engine's longHorizonBoost/volPremium width — adopting its idea would regress the two on-target horizons to help a minority of upward-drift days (textbook overfit). Every prior `switch` flag (06-16, 06-18, 07-06, 07-15) was likewise correctly kept and mean-reverted the next day. Shipped-engine calibration is sound: cov80 76.4 (7d, −3.6) / 80.4 (14d, on-target) / 80.6 (30d, on-target); the ~3.6pp 7d softness is the same weeks-long window/coin-mix soft patch (no safe short-end AI lever; volPremium is the only global width lever and is auto-tuner-owned — tuner ran today, applied no change, improvement 0, best cov80 79.7). meanPIT 0.541/0.566/0.572 sits at the upper edge of the engine's own "roughly unbiased" 0.43–0.57 band — the same window-driven upward drift that triggered the momentum flag; it was 0.45 on 07-16/07-17 and is not a structural regression; driftDamp (the drift lever) is auto-tuner-owned and held at 0.4. Conservative no-op on the model. selftest 3/3 (martingale 0.976) + require both exit 0

- 2026-07-15 AI daily: no change — calibration within tolerance. Shipped-engine cov80 is 76.8 (7d, −3.2) / 79.3 (14d, −0.7) / 78.9 (30d, −1.1) — all three horizons at or just below 80, aggregate running mildly light rather than a single off horizon. Not AI-actionable: the only global cov80 width lever is volPremium, which is auto-tuner-owned, and the tuner ran today and evaluated exactly the relevant move (volPremium 0.85→0.9, which would lift aggregate cov80 to 81.1) but DECLINED to apply it — improvement 0.37 (25.58→25.21 error), below its application threshold. Manually overriding volPremium to 0.9 would second-guess the tuner on a noise-level gain and thrash it (it would just be re-evaluated tomorrow), exactly the anti-pattern prior runs avoided. The AI-owned lever, longHorizonBoost (0.30), widens the LONG end via horizonFactor=1+boost·(H−7)/23 (=1.0 at 7d), but 14d/30d are the on-target horizons (within ~1pp of 80); the largest deficit is at 7d, the SHORT end where longHorizonBoost is 0 by construction, and no safe short-end AI lever exists — adding one is a structural change with overfitting risk against one offline snapshot (repeatedly judged so). 7d has oscillated 75.7–80.4 day-to-day over the past week (07-05 80.4, 07-06 75.7, 07-07 76.8, 07-09 77.6, 07-10 76.6, today 76.8), sitting ~76–77 — a persistent-but-modest window/coin-mix soft patch, not far-from-80. meanPIT 0.55/0.566/0.577; the 30d=0.577 upper-edge upward bias is the long-standing driftDamp effect (auto-tuner-owned, held at 0.4 this run), systemic across strategies at 30d and concentrated in idiosyncratic INJ/TIA outliers — not AI-actionable without overfitting. Tournament KEEP live_engine (#4 overall, mean score 29.1; best challenger Momentum 26.6, gap 2.5, inside the 3-pt switch margin). Conservative no-op on the model. selftest 3/3 (martingale 0.976) + require both exit 0

- 2026-07-10 AI daily: no change — calibration within tolerance at the two longer horizons; only 7d mildly light. Shipped-engine cov80 is on-target at 14d (80.9) and 30d (79.9), both within ~0.9pp of 80; 7d is 76.6, ~3.4pp light. Not AI-actionable: the single global width lever, volPremium, is auto-tuner-owned and the tuner ran today with an on-target aggregate (best cov80 79.9) and applied NO change (volPremium 0.85, driftDamp 0.4, improvement 0) — raising it globally to lift 7d would push the on-target 14d/30d over 80. No safe horizon-targeted short-end lever exists either: longHorizonBoost (0.30) widens the LONG end and is exactly 1.0 at 7d, and with the linear horizonFactor=1+boost·(H−7)/23, holding both 14d and 30d multipliers fixed mathematically pins volPremium=0.85 (no free parameter to add 7d width without disturbing the on-target horizons); adding a new short-end knob is a structural change with overfitting risk against one offline snapshot. The 7d deficit is a coin-mix effect, not a global bug — majors are on/over target at 7d (BTC 81, ETH 82, XRP 81, LINK 81, BNB 81, AVAX 86) while the shortfall lives in idiosyncratic alts (ARB 59, OP 68, INJ 72, SOL 74, APT 74); special-casing coins risks overfitting. 7d is also recent/window-driven (oscillated 75.7–80.4 day to day this past week: 07-05 80.4, 07-06 75.7, 07-07 76.8, 07-09 77.6, 07-10 76.6). meanPIT 0.541/0.553/0.571; the 30d=0.571 edge-of-band upward bias is the long-standing driftDamp effect (auto-tuner-owned at 0.4), systemic across strategies at 30d and concentrated in INJ/TIA outliers. Tournament KEEP live_engine (#1 overall 26.9; Momentum #2 at 27.0, inside the 3-pt switch margin). Conservative no-op on the model. selftest 3/3 (martingale 0.976) + require both exit 0

- 2026-07-09 AI daily: no change — calibration within tolerance (plus a repo-recovery: on entry the working tree was mid-`pull --rebase` with a corrupted conflict state replaying a stale ai-daily commit onto origin's fresh 2026-07-09 calibration; resolved by taking origin's fresh report data for every conflicted file and re-syncing master to origin's tip `19e2d05`, so the tool is committable and fully working again — no model code touched). Shipped-engine cov80 is on-target at both longer horizons (14d 79.9 / 30d 79.7, both within 0.3pp of 80); 7d is 77.6, ~2.4pp light but recovering monotonically from the 07-06/07 dip (73.1 → 73.0 → 75.4 → 77.6) toward its ~77–80 band — window noise, not a persistent structural deviation, and a global volPremium widen (the only cov80 lever) would push the on-target 14d/30d over 80, so not AI-actionable. meanPIT jumped to 0.551/0.553/0.574 today from ~0.45–0.49 all last week — a large one-day swing that confirms today's window carries upward drift, i.e. these are window-driven draws; the 30d=0.574 upper-edge bias is the long-standing driftDamp effect (auto-tuner-owned at 0.4, repeatedly judged overfitting-risk to special-case). Auto-tuner ran and applied NO change (volPremium 0.85 kept, improvement 0, best cov80 79.7). Tournament KEEP live_engine (#2 overall 27.5; best challenger block_boot 26.3, gap 1.2, well inside the 3-pt switch margin). Conservative no-op on the model. selftest 3/3 (martingale 0.976) + require both exit 0

- 2026-07-07 AI daily: no change — calibration within tolerance. cov80 is on-target at the two longer horizons (14d 80.2 / 30d 79.6, both within 0.4pp of 80) and 7d is 76.8, up from yesterday's 75.7 but still in the noisy 76–80 band it has oscillated through all week (07-06 75.7, 07-05 80.4, 06-30 79.3) — data-window noise, not a persistent deviation, so not actionable by an AI lever. meanPIT 0.541/0.546/0.570; the 30d=0.570 upper-edge upward bias is the long-standing driftDamp effect, is systemic across ALL strategies at 30d (gbm_t 0.559, gbm_normal 0.562, garch 0.571 — i.e. market drift in the window, not a model bug) and is concentrated in idiosyncratic outliers (INJ 0.68, TIA 0.60) — auto-tuner-owned (driftDamp at 0.4) and repeatedly judged overfitting-risk to special-case; only the bootstraps (which resample realized returns incl. drift) escape it, and block_boot is already routed for long horizons. Auto-tuner ran and applied NO change today (volPremium 0.85 kept, improvement 0, best cov80 79.3). Tournament KEEP live_engine (#2 overall 25.7; best challenger block_boot 25.1, gap 0.6, well inside the 3-pt switch margin). No AI-owned lever is clearly indicated; conservative no-op. selftest 3/3 (martingale 0.976) + require both exit 0

- 2026-07-06 AI daily: no change — calibration within tolerance. cov80 (the headline target) is on-target at the two longer horizons (14d 80.8 / 30d 80.0) and 7d dipped to 75.7 today, a single-day low against a stable 78–80 run over the prior week (07-05 80.4, 06-30 79.3, 06-29 79.0) — data-window noise, not a persistent deviation, so not actionable. meanPIT 0.52/0.54/0.57 all inside the roughly-unbiased 0.43–0.57 band; the 30d=0.565 upper-edge upward bias is the long-standing driftDamp effect concentrated in idiosyncratic outliers (INJ 0.72, TIA 0.64) — auto-tuner-owned (driftDamp already at 0.4) and repeatedly judged overfitting-risk to special-case. Auto-tuner ran and applied NO change today (volPremium 0.85 kept, improvement 0, best cov80 79.5). Tournament KEEP live_engine (#3 overall 25.2; best challengers gbm_t/momentum tied at 24.7, inside the 3-pt switch margin). No AI-owned lever is clearly indicated; conservative no-op. selftest 3/3 (martingale 0.976) + require both exit 0

- 2026-07-05 auto-tune: volPremium 0.9→0.85, driftDamp 0.4→0.4 (calibration error 22.59→21.9, cov80→79.7%)

- 2026-07-05 AI daily: no change — cov80 (the headline target) is essentially perfect at every horizon (7d 80.4 / 14d 81.8 / 30d 81.0) and meanPIT (0.52/0.53/0.55) sits inside the roughly-unbiased 0.43–0.57 band; live_engine #1 overall (score 23.5, gbm_t 23.6), tournament KEEP. Investigated the one persistent residual — cov50 runs ~3–6pp high (54.9/54.3/56.9) and cov90 ~1–2pp low (88.5/89.9/88.1) across 8+ days, the classic "center too wide, tails too thin" leptokurtic signature. Confirmed this is NOT fixable by the AI-owned Monte-Carlo levers: `backtestCalibration` computes PIT analytically via `normCdf(z)` (pure Gaussian), so `tDof` and the fat-tailed cone have ZERO effect on the reported cov50/cov90 (verified: tDof only feeds the live MC sim, and it has never been changed from 5). The signature is inherent to the Gaussian cross-check meeting fat-tailed crypto returns. The only true fix (a Student-t PIT inside `backtestCalibration`) would change the metric's semantics, break historical comparability, and shift the auto-tuner's own optimization target (it minimizes this error to tune volPremium) — out of bounds and destabilizing. `longHorizonBoost` (the other AI lever, which DOES affect the metric) is not indicated: cov80 is flat/on-target across horizons. driftDamp (the PIT-drift lever) is auto-tuner-owned and already at 0.4; the auto-tuner ACTED today (volPremium 0.9→0.85). Conservative no-op. selftest 3/3 (martingale 0.976) + require both exit 0

- 2026-07-03 auto-tune: volPremium 0.9→0.9, driftDamp 0.55→0.4 (calibration error 22.26→21.4, cov80→80.7%)

- 2026-07-03 AI daily: no change — aggregate calibration within tolerance (7d cov80=79.6%/14d 81.0%/30d 80.8%, all within ~1pp of 80; meanPIT 0.52/0.53/0.56, inside the roughly-unbiased band); live_engine #2 overall (score 22.8), only 0.4 behind gbm_t (22.4) — well inside the 3-pt switch margin, tournament KEEP. The deterministic auto-tuner ALREADY applied the relevant drift lever today (driftDamp 0.55→0.4, error 22.26→21.4), directly targeting the residual 30d meanPIT=0.558 upward bias — layering a manual drift change on top would thrash the fresh auto-tune and is unverifiable against the offline (no-network) backtest. driftDamp has oscillated 0.4↔0.55 for days (a scalar knob fighting a horizon-shaped bias) and is auto-tuner-owned. Residual bias stays concentrated in idiosyncratic per-coin outliers (INJ 30d PIT 0.72/cov80 62%, TIA 30d 0.63); special-casing risks overfitting. Conservative no-op. selftest 3/3 + require both exit 0

- 2026-07-02 auto-tune: volPremium 0.9→0.9, driftDamp 0.4→0.55 (calibration error 23.98→23.15, cov80→80.8%)

- 2026-07-02 AI daily: no change — calibration within tolerance (7d cov80=79/14d 82/30d 82, all within 2pp of 80; meanPIT 0.50/0.53/0.55, all inside the "roughly unbiased" 0.43–0.57 band); live_engine #2 overall (score 23.6), 0.4 behind Momentum (23.2) — well inside the 3-pt switch margin, tournament KEEP. The only residual is the long-standing horizon-growing meanPIT (0.50→0.53→0.55) upward drift, which is owned by the deterministic auto-tuner via driftDamp — and it ACTED this run (driftDamp 0.4→0.55). driftDamp has oscillated 0.4↔0.55 for days because a scalar drift knob is fighting a horizon-shaped bias; layering a manual drift/horizon change on top would thrash the fresh auto-tune and is unverifiable against the offline (no-network) backtest. Residual bias stays concentrated in idiosyncratic per-coin outliers (INJ 30d 0.65, TIA 30d 0.59); special-casing risks overfitting. Conservative no-op. selftest 3/3 + require both exit 0

- 2026-07-01 auto-tune: volPremium 0.95→0.9, driftDamp 0.55→0.4 (calibration error 21.82→21.27, cov80→79.5%)

- 2026-07-01 AI daily: no change — aggregate calibration within tolerance (7d cov80=80.1%/14d 81.4%/30d 80.7%, all within ~1.4pp of 80; meanPIT 0.51/0.53/0.56); live_engine #1 overall (score 22.3), tournament KEEP. The deterministic auto-tuner ALREADY applied the relevant drift lever today (driftDamp 0.55→0.4, volPremium 0.95→0.9, error 21.82→21.27), which directly targets the residual 30d meanPIT=0.56 upward bias — layering a manual drift change on top would thrash the fresh auto-tune and is unverifiable against the offline network backtest. That residual bias stays concentrated in idiosyncratic per-coin outliers (INJ 0.73, TIA 0.65); special-casing coins risks overfitting. Conservative no-op. selftest 3/3 + require both exit 0

- 2026-06-30 AI daily: no change — calibration within tolerance (7d cov80=79.3%/14d 81.6%/30d 83.2%); 7d/14d meanPIT near 0.50 (0.50/0.53); live_engine #3 overall (score 23.5), only 0.2 behind gbm_normal (23.3) — well inside the 3-pt switch margin, tournament KEEP; auto-tuner's best alt (volPremium 0.9) gained just 0.07 error and was not applied; the only residual is the long-standing 30d meanPIT=0.56 upward (bullish) bias, which is a drift effect owned by the deterministic auto-tuner (driftDamp=0.55) and concentrated in idiosyncratic outliers (INJ 0.73, TIA 0.65) — special-casing risks overfitting and an AI drift change would thrash the auto-tuner. Conservative no-op. selftest 3/3 + require both exit 0

- 2026-06-29 AI daily: no change — calibration within tolerance (7d 79%/14d 80%/30d 81%, all within 1pp of target); live_engine #1 overall (score 21.7), tournament KEEP; 7d/14d meanPIT near 0.50; residual 30d PIT=0.56 upward bias is stable and concentrated in INJ (0.73)/TIA (0.65) idiosyncratic outliers; auto-tuner's best drift alternative (driftDamp 0.4) gained only 0.18 error and was not applied, and driftDamp has oscillated 0.4↔0.55 for days, so no AI drift change is justified (overfitting/thrash risk). selftest 3/3 + require both exit 0

- 2026-06-28 auto-tune: volPremium 0.95→0.95, driftDamp 0.4→0.55 (calibration error 23.4→22.71, cov80→80.3%)

- 2026-06-28 AI daily: no change — calibration within tolerance (7d 78%/14d 80%/30d 83%); live_engine score 23.2, effectively tied with top cluster (~22.9), tournament KEEP; auto-tuner already raised driftDamp 0.4→0.55 today (error 23.4→22.71, PIT 0.526) which directly addresses the stable 30d PIT=0.56 upward bias, so no further drift change is justified; INJ/TIA remain idiosyncratic per-coin outliers (overfitting risk to special-case). selftest 3/3 + require both exit 0

- 2026-06-27 auto-tune: volPremium 0.95→0.95, driftDamp 0.55→0.4 (calibration error 21.81→21.02, cov80→79.2%)

- 2026-06-27 AI daily: no change — calibration within tolerance (7d 78%/14d 79%/30d 81%); live_engine #1 overall (score 22.3), tournament KEEP; auto-tuner already cut driftDamp 0.55→0.4 today (error 21.81→21.02, PIT 0.51) so no further drift change is justified; 30d PIT=0.56 stable upward bias, INJ/TIA remain idiosyncratic per-coin outliers (overfitting risk to special-case). selftest 3/3 + require both exit 0

- 2026-06-25 AI daily: no change — calibration within tolerance (7d 78%/14d 78%/30d 81%); live_engine #1 overall (score 22.4), tournament KEEP; 30d PIT=0.56 stable upward bias (not worsening); INJ remains idiosyncratic per-coin outlier (30d PIT=0.74)

- 2026-06-24 auto-tune: volPremium 1→0.95, driftDamp 0.55→0.55 (calibration error 23.2→22.49, cov80→79.3%)

- 2026-06-20 auto-tune: volPremium 1.05→1, driftDamp 0.7→0.55 (calibration error 24.35→23.23, cov80→79.4%)
- 2026-06-20 AI daily: no change — calibration within tolerance (7d 81%/14d 79%/30d 84%); auto-tuner already adjusted volPremium and driftDamp today; live_engine #1 overall (score 24.1)
- 2026-06-21 AI daily: no change — calibration within tolerance (7d 79%/14d 77%/30d 82%); live_engine #1 overall (score 23.1); 30d PIT=0.56 stable, no actionable drift

- 2026-06-14 auto-tune: volPremium 1.1→1.05, driftDamp 0.55→0.7 (calibration error 24.77→24.19, cov80→78.9%)
- 2026-06-14 AI daily: no change — calibration within tolerance (7d 82%/14d 79%/30d 83%); auto-tuner already adjusted volPremium and driftDamp today
- 2026-06-17 AI daily: no change — calibration within tolerance (7d 80%/14d 78%/30d 82%); 7d now perfect at target; live_engine #1 overall (score 23.2)

- 2026-06-13 auto-tune: volPremium 1→1.1, driftDamp 0.55→0.55 (calibration error 23.01→21.88, cov80→80%)
- 2026-06-13 AI daily: no change — auto-tuner already raised volPremium 1.0→1.1 today; awaiting next report to evaluate effect (cov80 currently 78%/76%/77% at 7d/14d/30d)

- 2026-06-07 auto-tune: volPremium 1.05→1, driftDamp 0.7→0.55 (calibration error 21.35→19.53, cov80→77.3%)

- 2026-06-07 auto-tune: volPremium 1.05→1.05, driftDamp 0.4→0.7 (calibration error 22.87→18.35, cov80→78%)
- 2026-06-07 AI daily: add longHorizonBoost=0.10 — horizon-dependent vol scaling to fix persistent 30d under-coverage (cov80 73% vs 80% target)
- 2026-06-07 AI daily: no change — 7d/14d within tolerance (82%/83%), 30d fix (longHorizonBoost) awaiting next report
- 2026-06-08 AI daily: longHorizonBoost 0.10→0.20 — 30d cov80 still 67% (13pp under target) after first boost; doubling horizon-dependent vol scaling
- 2026-06-09 AI daily: longHorizonBoost 0.20→0.30 — 30d cov80 improved to 76% but still 4pp under target; 7d/14d on target (80%/79%)
- 2026-06-10 AI daily: no change — awaiting fresh report to evaluate longHorizonBoost 0.30 effect; 7d/14d on target (80%/79%)
- 2026-06-11 AI daily: no change — report still dated 06-08 (pre-0.30 boost); awaiting fresh backtest to evaluate longHorizonBoost 0.30
- 2026-06-12 AI daily: no change — fresh report shows uniform ~2-3pp under-coverage (7d 78%/14d 77%/30d 78%); 30d improved from 76%→78% confirming boost effect; 7d drop is data-window shift (boost=0 at 7d)

- 2026-06-06 auto-tune: volPremium 1→1.05, driftDamp 0.55→0.4 (calibration error 26.44→25.18, cov80→78.3%)
